/**
 * Backward-compatible re-export.
 * All new code should import from @/components/ui/Skeleton directly.
 */
export {
    Skeleton,
    QuoteCardSkeleton,
    DashboardStatsSkeleton,
    QuoteListSkeleton,
    PlanCardSkeleton,
} from '@/components/ui/Skeleton';
